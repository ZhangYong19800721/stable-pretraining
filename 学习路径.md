学习 `stable_pretraining`，最有效的方法不是从 API 文档逐项背，而是采用：

> **先跑通一个最小实验 → 理解四个核心组件 → 改造 SimCLR → 学习在线评估 → 再进入 MAE、DINO、I-JEPA。**

先澄清一点：`stable_pretraining` 并不是 Meta FAIR 官方的 V-JEPA 训练库，而是 Galilai Group 开源的、基于 PyTorch Lightning 的基础模型与自监督学习研究框架。它目前集成了 SimCLR、BYOL、VICReg、DINO、DINOv2、MAE、I-JEPA、LeJEPA、CLIP 等多种方法。([GitHub][1])

---

# 一、学习前需要具备什么基础

开始前至少要掌握以下内容。

## 1. PyTorch基础

需要能独立写出：

```python
class Model(nn.Module):
    def __init__(self):
        super().__init__()

    def forward(self, x):
        ...
```

并理解：

* `Dataset`、`DataLoader`
* `nn.Module`
* `forward`
* 损失函数
* `loss.backward()`
* 优化器
* checkpoint
* GPU训练

不需要先精通 PyTorch 分布式训练。

## 2. Lightning基础

至少理解：

```python
class LitModel(L.LightningModule):
    def training_step(self, batch, batch_idx):
        ...

    def configure_optimizers(self):
        ...
```

以及：

```python
trainer = L.Trainer(...)
trainer.fit(model, datamodule)
```

这是因为 `stable_pretraining` 的训练调度、日志、回调和多 GPU 能力建立在 Lightning 之上。官方也明确说明其主要依赖包括 PyTorch、Lightning、Hugging Face 和 TorchMetrics。([GitHub][1])

## 3. 自监督学习基础

开始时只需要理解三个概念：

1. 同一张图像经过两次随机增强，得到两个 view。
2. 模型应该学习语义一致、增强不变的表示。
3. 表示是否有用，可以通过线性分类器或 KNN 测试。

暂时不要一上来研究 DINOv2、I-JEPA 或世界模型。

---

# 二、推荐的学习总路线

建议按以下顺序学习：

```text
监督学习
   ↓
SimCLR
   ↓
在线 Linear Probe / KNN
   ↓
VICReg 或 Barlow Twins
   ↓
BYOL / SimSiam
   ↓
MAE
   ↓
DINO
   ↓
I-JEPA / LeJEPA
   ↓
自定义方法
```

其中：

* **监督学习**：熟悉框架数据流。
* **SimCLR**：理解多视图、自监督损失和 projector。
* **VICReg**：理解防止表征坍塌。
* **BYOL**：理解 teacher-student 和 EMA。
* **MAE**：理解 masked image modeling。
* **DINO**：理解多裁剪、自蒸馏和 teacher centering。
* **I-JEPA**：进入联合嵌入预测架构。

---

# 三、第一阶段：安装并跑通官方教程

官方当前 PyPI 最新发布版本是 `0.1.8`，发布日期为 2026 年 7 月 8 日；官方仓库也提供源码安装和 CIFAR-10 SimCLR 教程。([PyPI][2])

你使用 Ubuntu 24.04 和 `uv`，建议这样创建环境。

```bash
git clone https://github.com/galilai-group/stable-pretraining.git
cd stable-pretraining

uv venv --python 3.11
source .venv/bin/activate

uv pip install -e .
```

测试导入：

```bash
python -c "import stable_pretraining as spt; print(spt)"
```

然后安装 Jupyter：

```bash
uv pip install jupyter
```

运行官方教程：

```bash
jupyter notebook examples/simclr_cifar10_tutorial.ipynb
```

官方将这个 notebook 定位为包含数据加载、Module、callbacks、训练和评估的完整入门教程。([GitHub][1])

## 第一阶段目标

你不需要立刻理解所有代码，只需要确认：

* CIFAR-10 能下载；
* 模型能开始训练；
* loss 能下降；
* Linear Probe 或 KNN 指标能输出；
* checkpoint 和日志能够生成。

---

# 四、第二阶段：理解框架的四个核心组件

`stable_pretraining` 的官方设计可以归纳成四个组件：

```text
DataModule
    ↓ batch
Module
    ↓ state dict
Callbacks
    ↓ metrics
Manager / Trainer
```

官方强调的关键思想是：**数据和模型中间状态都使用字典传递**。([GitHub][1])

---

## 1. Data：字典格式的数据

普通 PyTorch 数据集可能返回：

```python
return image, label
```

而 `stable_pretraining` 更强调返回：

```python
return {
    "image": image,
    "label": label,
}
```

自监督学习中可能是：

```python
return {
    "image": [view1, view2],
    "label": label,
}
```

这样做的好处是，后续组件不依赖元组中的固定位置，而是通过字段名读取数据。

你首先应该在训练代码中打印：

```python
batch = next(iter(train_loader))

print(batch.keys())
print(type(batch["image"]))
print(batch["label"].shape)
```

如果是多视图数据，再打印：

```python
print(len(batch["image"]))
print(batch["image"][0].shape)
print(batch["image"][1].shape)
```

### 学习目标

你必须能够回答：

* 原始图像字段叫什么？
* 标签字段叫什么？
* 两个增强视图放在哪里？
* batch 维度是多少？
* 增强是在 CPU 还是 GPU 上执行？

---

## 2. Module：模型与 forward 函数

典型结构类似：

```python
module = spt.Module(
    backbone=backbone,
    projector=projector,
    forward=spt.forward.simclr,
    simclr_loss=loss_fn,
)
```

这里要区分四个概念。

### Backbone

负责生成基础表示：

```text
image → ResNet / ViT → embedding
```

### Projector

把 backbone 表示映射到自监督损失空间：

```text
embedding → MLP → projection
```

### Forward函数

定义一次训练如何流动：

```text
两个增强视图
    ↓
backbone
    ↓
projector
    ↓
loss
```

### Loss

例如 SimCLR 使用 NT-Xent 对比损失。

官方提供了 `forward.simclr`、`forward.byol`、`forward.vicreg`、`forward.dino`、`forward.dinov2` 等现成 forward 函数。([GitHub][1])

---

## 3. State Dict：最重要的设计

这里的 state dict 不是通常意义上的 `model.state_dict()` 参数字典。

它是一次 forward 输出的中间结果，例如：

```python
{
    "loss": loss,
    "embedding": embedding,
    "projection": projection,
    "label": label,
}
```

你应该把它理解为：

> 当前 batch 经过模型后产生的所有可观察状态。

callbacks 可以读取这些状态，而不必修改训练循环。

这是学习 `stable_pretraining` 的核心。你后面设计新方法时，本质上就是：

1. 接收 batch 字典；
2. 运行模型；
3. 计算 loss；
4. 返回一个包含 loss 和中间张量的字典。

官方说明自定义 forward 只要返回类似 `{"loss": ..., "embedding": ...}` 的字典，就能接入其训练和 callback 系统。([GitHub][1])

---

## 4. Callbacks：在线评估

自监督训练中的 loss 较低，并不一定意味着表示质量高，因此需要额外评估。

重点先学两个 callback。

### OnlineProbe

在冻结的 embedding 上训练一个轻量线性分类器：

```text
embedding → Linear → class
```

它衡量表征是否线性可分。

### OnlineKNN

使用训练过程中缓存的 embedding 做 KNN 分类，不额外训练完整分类模型。

官方还提供 `RankMe` 等 callback，用于监控特征的有效秩；如果秩明显下降，可能意味着表示发生维度坍塌。([GitHub][1])

第一阶段不要把所有 callbacks 都学完，只学习：

* `OnlineProbe`
* `OnlineKNN`
* `RankMe`

---

# 五、第三阶段：完整拆解 SimCLR

SimCLR 是最适合入门的第一个算法。

你要理解完整数据流：

```text
原始图像 x
   ├── 随机增强 t1 → x1
   └── 随机增强 t2 → x2

x1 → backbone → h1 → projector → z1
x2 → backbone → h2 → projector → z2

z1、z2 → NT-Xent loss
```

## 必须理解的几个变量

| 变量            | 含义           |
| ------------- | ------------ |
| `x1, x2`      | 同一图像的两个增强视图  |
| `h1, h2`      | backbone 表示  |
| `z1, z2`      | projector 输出 |
| positive pair | 同一原图的两个视图    |
| negative pair | batch 中其他图像  |
| temperature   | 控制相似度分布的锐利程度 |

## 第一轮实验

直接跑官方配置，不修改算法。

```bash
spt examples/simclr_cifar10_config.yaml
```

`spt` CLI 使用 Hydra YAML 配置，并支持命令行覆盖参数，例如修改 epoch 或学习率。([PyPI][2])

例如：

```bash
spt examples/simclr_cifar10_config.yaml \
    trainer.max_epochs=20 \
    module.optim.lr=0.01
```

## 第二轮实验：改 batch size

分别测试：

```text
batch_size = 64
batch_size = 128
batch_size = 256
```

记录：

* 显存占用
* 单 epoch 时间
* 最终 KNN accuracy
* 最终 probe accuracy
* loss 曲线

## 第三轮实验：改增强

依次去掉：

* `ColorJitter`
* `RandomGrayscale`
* `RandomResizedCrop`

观察 representation quality 怎么变化。

这个实验会让你真正理解：

> 自监督学习的监督信号很大程度上来自数据增强设计。

## 第四轮实验：改 backbone

例如：

```python
spt.backbone.from_torchvision("resnet18")
```

替换成：

```python
spt.backbone.from_torchvision("resnet50")
```

或者：

```python
spt.backbone.from_timm("vit_tiny_patch16_224")
```

官方支持从 torchvision、timm 和 Hugging Face 加载 backbone，也提供 MLP、ViT、MaskedEncoder、MAEDecoder、IJEPAMasking 等组件。([PyPI][2])

---

# 六、第四阶段：自己写一个最小 forward

不要长期停留在调用：

```python
forward=spt.forward.simclr
```

你应该仿照官方函数，自己写一个简化版本。

伪代码如下：

```python
def my_simclr_forward(self, batch, stage):
    x1, x2 = batch["image"]

    h1 = self.backbone(x1)
    h2 = self.backbone(x2)

    z1 = self.projector(h1)
    z2 = self.projector(h2)

    loss = self.simclr_loss(z1, z2)

    return {
        "loss": loss,
        "embedding": torch.cat([h1, h2], dim=0),
        "projection": torch.cat([z1, z2], dim=0),
        "label": torch.cat(
            [batch["label"], batch["label"]],
            dim=0,
        ),
    }
```

具体签名需要以你当前安装版本的源码为准，但学习重点是数据流，而不是复制函数名。

## 本阶段目标

你应该能独立回答：

* 为什么 probe 通常读取 `embedding` 而不是 `projection`？
* 为什么 loss 用 projector 输出？
* 为什么 callback 可以读到 embedding？
* 为什么返回字典比返回单个 loss 更灵活？
* 某个字段名字改变后，哪些 callback 要同步修改？

---

# 七、第五阶段：学习 VICReg，而不是马上学 DINO

完成 SimCLR 后，推荐第二个学习 VICReg。

VICReg 的损失通常分为三部分：

```text
Invariance
Variance
Covariance
```

分别用于：

* 拉近同一图像不同视图的表示；
* 防止所有样本输出完全相同；
* 降低特征维度间的冗余。

它比 DINO 更容易理解，同时能直接引出自监督学习中的关键问题：

> representation collapse。

你可以做以下实验：

1. 只保留 invariance loss；
2. 加入 variance loss；
3. 再加入 covariance loss；
4. 用 `RankMe` 观察表征秩变化。

官方提供 VICReg 的 forward 和 loss，也将 `RankMe` 作为表示有效秩监控工具。([GitHub][1])

---

# 八、第六阶段：学习 Teacher–Student 方法

接下来学习：

```text
SimSiam → BYOL → DINO
```

## SimSiam

重点理解：

* Siamese network
* predictor
* stop-gradient
* 不使用负样本

## BYOL

重点理解：

* online encoder
* target encoder
* exponential moving average
* predictor
* stop-gradient

数据流是：

```text
view 1 → online encoder → predictor
                            ↓ match
view 2 → target encoder ─────────
```

target encoder 不直接通过普通反向传播更新，而是通过在线网络参数的 EMA 更新。

## DINO

重点理解：

* student network
* teacher network
* EMA teacher
* multi-crop
* centering
* sharpening
* teacher temperature

不要把 DINO 仅仅看成“另一个模型”，它实际上是在训练机制上把：

```text
数据增强 + teacher-student + 多裁剪 + 防坍塌机制
```

组合起来。

---

# 九、第七阶段：进入 MAE 与 I-JEPA

这一步和你的世界模型、V-JEPA 研究方向最相关。

## 先学 MAE

数据流：

```text
image
  ↓ patchify
patch tokens
  ↓ mask
visible tokens
  ↓ encoder
latent representation
  ↓ decoder
reconstruct masked pixels
```

需要理解：

* patch embedding
* random masking
* encoder
* lightweight decoder
* pixel reconstruction loss
* mask ratio

## 再学 I-JEPA

数据流：

```text
context blocks
     ↓
context encoder
     ↓
predictor
     ↓
预测 target block 的 latent representation
     ↑
target encoder
```

核心区别：

| 方法     | 预测目标                     |
| ------ | ------------------------ |
| MAE    | 像素                       |
| I-JEPA | latent representation    |
| V-JEPA | 视频 latent representation |

`stable_pretraining` 仓库提供完整方法实现目录，其中包括 MAE、I-JEPA、LeJEPA 等方法；官方 README 还列出了 30 多种 SSL、监督和多模态训练方案。([PyPI][2])

---

# 十、源码阅读顺序

不要从仓库根目录逐文件读。推荐按下面顺序。

## 第1步：官方教程

```text
examples/simclr_cifar10_tutorial.ipynb
```

目的是建立整体印象。

## 第2步：完整配置

```text
examples/simclr_cifar10_config.yaml
```

重点看：

* dataset
* transforms
* backbone
* projector
* loss
* optimizer
* callbacks
* trainer

## 第3步：核心 Module

进入：

```text
stable_pretraining/
```

查找：

```python
class Module
```

重点读：

* `__init__`
* `forward`
* `training_step`
* `validation_step`
* `configure_optimizers`

## 第4步：SimCLR forward

查找：

```python
def simclr
```

追踪：

```text
batch
  ↓
backbone
  ↓
projector
  ↓
loss
  ↓
state dict
```

## 第5步：NT-Xent Loss

查找：

```python
class NTXEntLoss
```

理解：

* 特征归一化；
* 相似度矩阵；
* 正样本索引；
* mask；
* temperature；
* cross entropy。

## 第6步：OnlineProbe

查找：

```python
class OnlineProbe
```

重点看它如何：

* 从 state 字典读取 embedding；
* detach 主网络表示；
* 训练独立 probe；
* 记录准确率；
* 避免 probe 梯度影响 backbone。

## 第7步：方法目录

然后阅读：

```text
stable_pretraining/methods/
```

推荐顺序：

```text
SimCLR
VICReg
SimSiam
BYOL
MAE
DINO
IJEPA
LeJEPA
```

仓库根目录的 `METHODS.md` 也是官方提供的方法索引入口。([GitHub][1])

---

# 十一、建议的六周学习安排

## 第1周：框架基础

学习：

* 安装；
* 官方 notebook；
* dict batch；
* `spt.Module`；
* Lightning Trainer；
* Manager；
* checkpoint。

成果：

> 完整跑通 CIFAR-10 监督学习和 SimCLR。

## 第2周：SimCLR

学习：

* MultiViewTransform；
* backbone；
* projector；
* NT-Xent；
* temperature；
* batch size；
* OnlineKNN；
* OnlineProbe。

成果：

> 自己写一个简化版 SimCLR forward。

## 第3周：VICReg和坍塌问题

学习：

* invariance；
* variance；
* covariance；
* RankMe；
* 特征分布；
* 表示有效秩。

成果：

> 对比 SimCLR 与 VICReg 的表示质量。

## 第4周：Teacher–Student

学习：

* stop-gradient；
* EMA；
* teacher-student；
* BYOL；
* SimSiam；
* DINO基础。

成果：

> 能画出 BYOL 和 DINO 的完整数据流图。

## 第5周：Masked Modeling

学习：

* ViT；
* patch embedding；
* mask token；
* MAE；
* decoder；
* reconstruction loss。

成果：

> 在 CIFAR-10 或 Imagenette 上运行 MAE。

## 第6周：JEPA

学习：

* context encoder；
* target encoder；
* predictor；
* target masking；
* latent prediction；
* I-JEPA；
* LeJEPA。

成果：

> 修改 I-JEPA 的 mask 策略或 predictor，并完成一次对照实验。

---

# 十二、最适合你的三个练习项目

## 项目一：CIFAR-10自监督方法对比

比较：

```text
SimCLR
VICReg
BYOL
MAE
```

统一使用：

* 相同 backbone；
* 相同 epoch；
* 相近 batch size；
* 相同 Linear Probe；
* 相同 KNN。

输出：

| 方法     | 参数量 | 训练时间 | KNN | Linear Probe |
| ------ | --: | ---: | --: | -----------: |
| SimCLR |     |      |     |              |
| VICReg |     |      |     |              |
| BYOL   |     |      |     |              |
| MAE    |     |      |     |              |

## 项目二：Imagenette上的ViT预训练

使用：

* Imagenette；
* ViT-S/16 或 ViT-Tiny；
* SimCLR；
* MAE；
* I-JEPA。

仓库官方 benchmark 本身也使用 Imagenette、ViT-S/16 和统一在线 probe 对多种方法进行 sanity-check 对比，因此它是很合适的小规模实验数据集。([GitHub][1])

## 项目三：第一人称视频帧预训练

结合你的研究方向：

1. 从 Ego 视频均匀抽帧；
2. 建立 Hugging Face Dataset 或普通 Dataset；
3. 先把每帧当静态图像；
4. 用 SimCLR 或 VICReg 预训练；
5. 用动作类别、场景类别或对象类别做 Linear Probe；
6. 最后再升级到相邻帧和视频块预测。

不要第一步就做 V-JEPA。先证明：

> 你的数据管线、增强方式、backbone 和在线评估都正确。

---

# 十三、学习时暂时不要碰的内容

初学阶段暂时略过：

* JAX后端；
* SLURM自动重排队；
* DeepSpeed；
* 多节点训练；
* 大规模 sweep；
* 自定义 registry；
* DINOv2完整训练；
* 视频 backbone；
* 分布式 sinkhorn；
* world model planning。

官方当前 JAX 后端仍标记为实验性，而且相对 PyTorch 路径，DINO、DINOv2、MAE、I-JEPA 和视频 backbone 等功能仍未全部覆盖，因此你的主线应当选择 PyTorch/Lightning 后端。([PyPI][2])

---

# 十四、判断是否真正学会的标准

当你能完成下面这些任务，就算基本掌握了：

1. 不看教程创建一个 dict 格式 Dataset；
2. 为每张图像生成两个增强视图；
3. 配置 ResNet 或 ViT backbone；
4. 自己实现一个 forward 函数；
5. 返回 loss、embedding 和其他中间状态；
6. 接入 OnlineProbe 和 OnlineKNN；
7. 修改 optimizer 和 scheduler；
8. 从 checkpoint 恢复训练；
9. 用 YAML 配置启动实验；
10. 加入一个新的 loss 或自监督方法。

你当前最合适的起点是：

```text
官方 CIFAR-10 SimCLR notebook
        ↓
打印 batch 和 state dict
        ↓
自己重写 SimCLR forward
        ↓
加入 OnlineProbe、OnlineKNN、RankMe
        ↓
复现 VICReg
        ↓
进入 MAE 和 I-JEPA
```

不要从 I-JEPA 或 LeJEPA 开始。先把 **SimCLR 的数据、forward、state dict、loss 和 callback** 五条链路彻底走通，后续方法主要就是替换网络结构、目标函数和 teacher-student 更新机制。

[1]: https://github.com/galilai-group/stable-pretraining "GitHub - galilai-group/stable-pretraining: Reliable, minimal and scalable library for pretraining foundation and world models · GitHub"
[2]: https://pypi.org/project/stable-pretraining/?utm_source=chatgpt.com "stable-pretraining · PyPI"
